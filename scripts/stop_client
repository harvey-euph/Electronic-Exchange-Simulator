#!/bin/bash

echo "Stopping all Exchange Clients..."

# Kill all processes whose names start with cli_
pkill -f "build/app/cli_"

if [ $? -eq 0 ]; then
    echo "Successfully sent termination signal to all clients."
else
    echo "No clients found running."
fi

echo "Done."
